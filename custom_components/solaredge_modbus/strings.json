{
  "config": {
    "title": "ABB SunSpec Modbus",
    "step": {
      "user": {
        "title": "Define your ABB SunSpec modbus connection",
        "data": {
          "host": "The IP address of your ABB SunSpec device",
          "name": "The prefix to be used for your ABB SunSpec sensors",
          "port": "The TCP port on which to connect to the ABB SunSpec",
          "scan_interval": "The polling frequentie of the modbus registers in seconds"
        }
      }
    },
    "error": {
      "already_configured": "Device is already configured"
    },
    "abort": {
      "already_configured": "Device is already configured"
    }
  }
}
